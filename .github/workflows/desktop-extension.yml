name: Build Desktop XPI and Windows EXE

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'desktop/**'
      - 'shared/**'
      - '.github/workflows/desktop-extension.yml'
  pull_request:
    branches: [main]
    paths:
      - 'desktop/**'
      - 'shared/**'
      - '.github/workflows/desktop-extension.yml'

permissions:
  contents: write

concurrency:
  group: fenix-privacy-desktop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  extension:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      release_tag: ${{ steps.version.outputs.release_tag }}
      signed: ${{ steps.sign.outputs.signed }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Stamp monotonic desktop version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          # AMO rejects an already-submitted version. Include the run attempt so
          # rerunning a job after a downstream failure always submits a newer XPI.
          VERSION="3.0.${GITHUB_RUN_NUMBER}.${GITHUB_RUN_ATTEMPT}"
          node - "$VERSION" <<'NODE'
          const fs = require('fs');
          const file = 'desktop/firefox-extension/manifest.json';
          const manifest = JSON.parse(fs.readFileSync(file, 'utf8'));
          manifest.version = process.argv[2];
          fs.writeFileSync(file, JSON.stringify(manifest, null, 2) + '\n');
          NODE
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "release_tag=desktop-v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Install Mozilla web-ext
        run: npm install --global web-ext@10

      - name: Run desktop tests
        run: node desktop/tests/matcher.test.cjs

      - name: Lint self-hosted extension
        run: web-ext lint --self-hosted --source-dir desktop/firefox-extension

      - name: Build unsigned XPI
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts
          web-ext build \
            --source-dir desktop/firefox-extension \
            --artifacts-dir artifacts \
            --overwrite-dest
          BUILT="$(find artifacts -maxdepth 1 -type f -name '*.zip' -print -quit)"
          test -n "$BUILT"
          mv "$BUILT" artifacts/Fenix-Privacy-Desktop-unsigned.xpi

      - name: Sign XPI with Mozilla AMO
        if: github.event_name != 'pull_request'
        id: sign
        env:
          WEB_EXT_API_KEY: ${{ secrets.AMO_API_KEY }}
          WEB_EXT_API_SECRET: ${{ secrets.AMO_API_SECRET }}
        shell: bash
        run: |
          set -euo pipefail
          echo "signed=false" >> "$GITHUB_OUTPUT"

          # AMO JWT issuer/secret never contain whitespace. Remove any spaces or
          # line breaks accidentally copied into GitHub Actions secrets.
          API_KEY="$(printf '%s' "${WEB_EXT_API_KEY:-}" | tr -d '[:space:]')"
          API_SECRET="$(printf '%s' "${WEB_EXT_API_SECRET:-}" | tr -d '[:space:]')"

          if [[ -z "$API_KEY" || -z "$API_SECRET" ]]; then
            echo "::error::AMO signing is mandatory for main-branch releases. Add both AMO_API_KEY (JWT issuer) and AMO_API_SECRET (JWT secret) as repository Actions secrets."
            exit 1
          fi

          echo "AMO credentials detected. Requesting an unlisted Mozilla signature."
          rm -rf signed
          mkdir -p signed
          set +e
          web-ext sign \
            --source-dir desktop/firefox-extension \
            --artifacts-dir signed \
            --channel unlisted \
            --approval-timeout 1800000 \
            --no-input \
            --api-key "$API_KEY" \
            --api-secret "$API_SECRET" 2>&1 | tee "$RUNNER_TEMP/web-ext-sign.log"
          status=${PIPESTATUS[0]}
          set -e

          if ((status != 0)); then
            echo "::error::Mozilla AMO signing failed. The repository secrets were present; see the web-ext error above for the actual AMO/API failure."
            exit "$status"
          fi

          mapfile -t SIGNED_FILES < <(find signed -maxdepth 1 -type f -name '*.xpi' -print)
          if (("${#SIGNED_FILES[@]}" != 1)); then
            echo "::error::Expected exactly one Mozilla-signed XPI, found ${#SIGNED_FILES[@]}."
            exit 1
          fi
          SIGNED="${SIGNED_FILES[0]}"

          unzip -tqq "$SIGNED"
          if ! unzip -Z1 "$SIGNED" | grep -Eiq '^META-INF/(mozilla\.rsa|cose\.sig)$'; then
            echo "::error::AMO returned an XPI without a recognizable Mozilla signature block."
            exit 1
          fi
          if cmp -s "$SIGNED" artifacts/Fenix-Privacy-Desktop-unsigned.xpi; then
            echo "::error::The AMO result is byte-identical to the unsigned package."
            exit 1
          fi

          cp "$SIGNED" artifacts/Fenix-Privacy-Desktop.xpi
          rm -f artifacts/Fenix-Privacy-Desktop-unsigned.xpi
          sha256sum artifacts/Fenix-Privacy-Desktop.xpi
          echo "signed=true" >> "$GITHUB_OUTPUT"

      - name: Upload XPI handoff
        uses: actions/upload-artifact@v4
        with:
          name: desktop-xpi-${{ steps.version.outputs.version }}
          retention-days: 7
          if-no-files-found: error
          path: artifacts/*.xpi

  windows-installer:
    needs: extension
    runs-on: windows-latest
    outputs:
      authenticode_signed: ${{ steps.authenticode.outputs.signed }}

    steps:
      - uses: actions/checkout@v4

      - name: Download XPI
        uses: actions/download-artifact@v4
        with:
          name: desktop-xpi-${{ needs.extension.outputs.version }}
          path: artifacts

      - name: Select XPI for installer
        id: package
        shell: pwsh
        run: |
          $signed = "${{ needs.extension.outputs.signed }}" -eq "true"
          if ($signed) {
            $xpi = Resolve-Path "artifacts/Fenix-Privacy-Desktop.xpi"
            "output=Fenix-Privacy-Desktop-Installer" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          } else {
            $xpi = Resolve-Path "artifacts/Fenix-Privacy-Desktop-unsigned.xpi"
            "output=Fenix-Privacy-Desktop-DEV-Installer" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          }
          "xpi=$($xpi.Path)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --no-progress -y

      - name: Build Windows installer EXE
        shell: pwsh
        run: |
          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          if (-not (Test-Path $iscc)) {
            $iscc = "${env:ProgramFiles}\Inno Setup 6\ISCC.exe"
          }
          if (-not (Test-Path $iscc)) {
            throw "ISCC.exe not found after installing Inno Setup"
          }

          New-Item -ItemType Directory -Force -Path out | Out-Null
          & $iscc `
            "/DMyAppVersion=${{ needs.extension.outputs.version }}" `
            "/DXpiSource=${{ steps.package.outputs.xpi }}" `
            "/DOutputBase=${{ steps.package.outputs.output }}" `
            "desktop/windows/FenixPrivacyInstaller.iss"

          if ($LASTEXITCODE -ne 0) {
            throw "Inno Setup failed with exit code $LASTEXITCODE"
          }

          Get-ChildItem "out\*.exe"

      - name: Authenticode sign and verify Windows installer when configured
        id: authenticode
        shell: pwsh
        env:
          WINDOWS_SIGNING_PFX_BASE64: ${{ secrets.WINDOWS_SIGNING_PFX_BASE64 }}
          WINDOWS_SIGNING_PFX_PASSWORD: ${{ secrets.WINDOWS_SIGNING_PFX_PASSWORD }}
        run: |
          "signed=false" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          $encoded = ($env:WINDOWS_SIGNING_PFX_BASE64 -replace '\s', '')
          $password = $env:WINDOWS_SIGNING_PFX_PASSWORD
          if ([string]::IsNullOrWhiteSpace($encoded) -or [string]::IsNullOrEmpty($password)) {
            Write-Host "::notice::Authenticode certificate not configured. The EXE will remain unsigned; the bundled XPI is still Mozilla-signed on production builds."
            exit 0
          }

          $pfx = Join-Path $env:RUNNER_TEMP "fenix-privacy-authenticode.pfx"
          try {
            [IO.File]::WriteAllBytes($pfx, [Convert]::FromBase64String($encoded))
            $signtool = Get-ChildItem "${env:ProgramFiles(x86)}\Windows Kits\10\bin" `
              -Filter signtool.exe -File -Recurse |
              Where-Object { $_.FullName -match '\\x64\\signtool\.exe$' } |
              Sort-Object FullName -Descending |
              Select-Object -First 1
            if ($null -eq $signtool) {
              throw "signtool.exe was not found in the Windows SDK"
            }
            $exe = (Get-ChildItem "out\*.exe" -File | Select-Object -First 1).FullName
            if ([string]::IsNullOrWhiteSpace($exe)) {
              throw "Windows installer was not found"
            }

            & $signtool.FullName sign /fd SHA256 /td SHA256 `
              /tr "http://timestamp.digicert.com" /f $pfx /p $password $exe
            if ($LASTEXITCODE -ne 0) {
              throw "signtool sign failed with exit code $LASTEXITCODE"
            }
            & $signtool.FullName verify /pa /all /v $exe
            if ($LASTEXITCODE -ne 0) {
              throw "signtool verification failed with exit code $LASTEXITCODE"
            }
            $signature = Get-AuthenticodeSignature $exe
            if ($signature.Status -ne [System.Management.Automation.SignatureStatus]::Valid) {
              throw "Authenticode status is $($signature.Status), expected Valid"
            }
            "signed=true" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          } finally {
            Remove-Item $pfx -Force -ErrorAction SilentlyContinue
          }

      - name: Assemble desktop artifacts
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path final | Out-Null
          Copy-Item artifacts\*.xpi final\
          Copy-Item out\*.exe final\
          @{
            version = "${{ needs.extension.outputs.version }}"
            xpiMozillaSigned = ("${{ needs.extension.outputs.signed }}" -eq "true")
            exeAuthenticodeSigned = ("${{ steps.authenticode.outputs.signed }}" -eq "true")
          } | ConvertTo-Json | Set-Content final\desktop-signing.json -Encoding utf8

      - name: Upload Windows desktop artifact
        uses: actions/upload-artifact@v4
        with:
          name: Fenix-Privacy-Desktop-${{ needs.extension.outputs.version }}
          retention-days: 30
          if-no-files-found: error
          path: final/*

  publish:
    if: github.event_name != 'pull_request' && needs.extension.outputs.signed == 'true'
    needs: [extension, windows-installer]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download final desktop artifact
        uses: actions/download-artifact@v4
        with:
          name: Fenix-Privacy-Desktop-${{ needs.extension.outputs.version }}
          path: final

      - name: Publish desktop GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.extension.outputs.version }}
          SIGNED: ${{ needs.extension.outputs.signed }}
          EXE_SIGNED: ${{ needs.windows-installer.outputs.authenticode_signed }}
          RELEASE_TAG: ${{ needs.extension.outputs.release_tag }}
        shell: bash
        run: |
          set -euo pipefail

          [[ "$SIGNED" == "true" ]] || {
            echo "Refusing to publish an unsigned Firefox package." >&2
            exit 1
          }

          if [[ "$EXE_SIGNED" == "true" ]]; then
            EXE_NOTE="The Windows installer is Authenticode-signed and verified; it installs the same Mozilla-signed XPI."
          else
            EXE_NOTE="The Windows installer is not Authenticode-signed because no code-signing certificate was configured; it contains the Mozilla-signed XPI and Firefox still performs its normal add-on confirmation."
          fi

          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release upload "$RELEASE_TAG" \
              final/Fenix-Privacy-Desktop.xpi \
              final/Fenix-Privacy-Desktop-Installer.exe \
              final/desktop-signing.json \
              --clobber
          else
            gh release create "$RELEASE_TAG" \
              final/Fenix-Privacy-Desktop.xpi \
              final/Fenix-Privacy-Desktop-Installer.exe \
              final/desktop-signing.json \
              --latest=false \
              --title "Fenix Privacy Desktop $VERSION" \
              --notes "Production release: Mozilla-signed Firefox extension for Windows, macOS and Linux, plus the Windows installer EXE. $EXE_NOTE"
          fi

          ASSETS="$(gh release view "$RELEASE_TAG" --json assets --jq '.assets[].name')"
          printf '%s\n' "$ASSETS"
          grep -Fxq 'Fenix-Privacy-Desktop.xpi' <<< "$ASSETS"
          grep -Fxq 'Fenix-Privacy-Desktop-Installer.exe' <<< "$ASSETS"
          grep -Fxq 'desktop-signing.json' <<< "$ASSETS"

      - name: Publish Firefox self-update manifest
        env:
          VERSION: ${{ needs.extension.outputs.version }}
          RELEASE_TAG: ${{ needs.extension.outputs.release_tag }}
        shell: bash
        run: |
          set -euo pipefail

          SHA256="$(sha256sum final/Fenix-Privacy-Desktop.xpi | awk '{print $1}')"
          UPDATE_LINK="https://github.com/${GITHUB_REPOSITORY}/releases/download/${RELEASE_TAG}/Fenix-Privacy-Desktop.xpi"
          export SHA256 UPDATE_LINK

          git fetch origin main
          git checkout main
          git reset --hard origin/main

          python3 - <<'PY'
          import json, os
          data = {
              "addons": {
                  "fenix-privacy-desktop@astropuzzo.github.io": {
                      "updates": [{
                          "version": os.environ["VERSION"],
                          "update_link": os.environ["UPDATE_LINK"],
                          "update_hash": "sha256:" + os.environ["SHA256"],
                          "applications": {
                              "gecko": {"strict_min_version": "142.0"}
                          },
                      }]
                  }
              }
          }
          with open("desktop-updates.json", "w", encoding="utf-8") as f:
              json.dump(data, f, indent=2)
              f.write("\n")
          PY

          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add desktop-updates.json
          if git diff --cached --quiet; then
            echo "Desktop update feed is already current."
            exit 0
          fi
          git commit -m "chore: update desktop add-on feed [skip ci]"

          for attempt in 1 2 3; do
            git pull --rebase origin main
            if git push origin HEAD:main; then
              exit 0
            fi
            if ((attempt == 3)); then
              echo "Unable to publish desktop update feed after 3 attempts." >&2
              exit 1
            fi
            sleep "$((attempt * 5))"
          done
